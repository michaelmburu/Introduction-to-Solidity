//import solidity
pragma solidity ^*;

//contract
contract Simple{
    //reserved variables ether sazbo finney
    bool isEqual = (1 ether == 5000 finney);

    //time units
    bool isTime = (1 == 1 seconds);

    //seconds, minutes, hours, days, weeks
    bool isTime = (1 hours == 60 minutes)
    
}
